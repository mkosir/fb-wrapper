// import FacebookClient from '../FacebookClient';
// const facebookClient = new FacebookClient(facebookToken, facebookAppID, facebookAppSecret);

// test('FacebookClient - postOnWall', () => {
//   expect(facebookClient.postOnWall('Test message!')).toBe('Test message!');
// });

test('Dummy - check addition', () => {
  expect(2 + 2).toBe(4);
});
