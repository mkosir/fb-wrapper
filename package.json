{
  "name": "fb-wrapper",
  "version": "1.0.8",
  "description": "Facebook API client for Node.js",
  "license": "MIT",
  "repository": "mkosir/fb-wrapper",
  "main": "lib/FacebookClient.js",
  "types": "lib/FacebookClient.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "clean": "rimraf lib",
    "test": "jest --config jestconfig.json",
    "build": "tsc",
    "prettier-format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepublish-clean": "npm run clean && npm run lint && npm run build"
  },
  "keywords": [
    "facebook",
    "client",
    "fb-wrapper"
  ],
  "bugs": {
    "url": "https://github.com/mkosir/fb-wrapper/issues"
  },
  "homepage": "https://github.com/mkosir/fb-wrapper#readme",
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "tslint": "^5.17.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "fb": "^2.0.0"
  }
}
